- cat_scope = @human.cycle_actions.for_cycle(@cycle).live.active.where(category: @cycle_action.category)

= turbo_stream.replace "category_#{@cycle_action.category}_list" do
  = render "cycle_actions/sorted_list", actions: @category_actions, category: @cycle_action.category, human: @human, next_cycle: @cycle.next_cycle

= turbo_stream.replace "hours_total" do
  = render "cycle_actions/hours_total", total_hours: @total_hours, human: @human, cycle: @cycle

= turbo_stream.replace "category_#{@cycle_action.category}_count" do
  = render "cycle_actions/category_count", category: @cycle_action.category, count: cat_scope.count, hours: cat_scope.sum(:hours)

= turbo_stream.replace "new_cycle_action_form" do
  = render "cycle_actions/form", cycle_action: CycleAction.new(human_id: @human.id, cycle_id: @cycle.id), human: @human, cycle: @cycle, flash_success: true
