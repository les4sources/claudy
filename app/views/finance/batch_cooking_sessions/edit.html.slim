- content_for :page_header do
  = render "layouts/components/page_header",
           title: @session.title,
           links: [ \
             link_to("← Toutes les sessions", finance_batch_cooking_sessions_path, class: "inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 shadow-sm hover:bg-gray-50") \
           ]

= render "form"
