/ La fiche d'un objet, servie dans la Turbo Frame de la carte (epic #348,
  phase 2). Un objet neuf (`new`) passe par la même vue.
= turbo_frame_tag "feature_panel" do
  = render "maps/feature_panel", feature: @feature
